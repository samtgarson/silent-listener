require 'spec_helper'

describe SilentListener do
  it 'has a version number' do
    expect(SilentListener::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
